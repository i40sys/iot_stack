all:
  children:
    iot-gw-hyperv:
      hosts:
        k5.joor.net:
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    # ansible_user: 'oriol'
    #ansible_ssh_pass: ''
    #ansible_port: 5555
    #ansible_connection: 'ssh'
